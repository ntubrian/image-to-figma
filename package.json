{
  "name": "image-to-figma",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/plugin"
  ],
  "scripts": {
    "dev": "npm -w packages/server run dev",
    "build": "npm -w packages/shared run build && npm -w packages/server run build && npm -w packages/plugin run build"
  }
}
