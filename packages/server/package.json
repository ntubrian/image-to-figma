{
  "name": "@image-to-figma/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -p ../shared/tsconfig.json && if [ -f .env ]; then node --env-file=.env src/index.ts; else node src/index.ts; fi",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@image-to-figma/shared": "file:../shared",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
