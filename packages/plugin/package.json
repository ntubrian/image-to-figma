{
  "name": "@image-to-figma/plugin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p ../shared/tsconfig.json && node scripts/build.mjs",
    "watch": "tsc -p ../shared/tsconfig.json && node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@image-to-figma/shared": "file:../shared"
  }
}
